import 'package:flutter/material.dart';

class BranchesActivity extends StatelessWidget {
  const BranchesActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
